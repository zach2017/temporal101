# Auto-generated docker-compose for OrderProcessingSystem
version: "3.9"

services:
  order-worker:
    build: .
    command: python worker_order_worker.py
    deploy:
      replicas: 3
      resources:
        limits:
          cpus: '1'
          memory: 1G
    restart: unless-stopped

  payment-worker:
    build: .
    command: python worker_payment_worker.py
    deploy:
      replicas: 2
      resources:
        limits:

          memory: 512M
    restart: unless-stopped

  fulfillment-worker:
    build: .
    command: python worker_fulfillment_worker.py
    deploy:
      replicas: 2
      resources:
        limits:
          memory: "512M"
    restart: unless-stopped
