click>=8.1
pdfplumber>=0.11
PyMuPDF>=1.25
pytesseract>=0.3.13
Pillow>=11.1

# MIME detection — pick ONE depending on your OS:
#   Linux/macOS (with libmagic installed): python-magic>=0.4.27
#   Windows (bundles the DLL):            python-magic-bin>=0.4.14
# If neither is installed the CLI still works via extension-based detection.
